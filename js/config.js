/* ============================================
   config.js - Configuracion para GitHub Pages
   ============================================

   En GitHub Pages NO hay servidor backend.
   Los datos se guardan en localStorage del navegador.

   Si quieres conectar a Google Sheets mas adelante,
   necesitamos usar SheetDB o Firebase (te lo explico).
*/

export const API_URL = '';
export const MODO_LOCAL = true;
